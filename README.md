# ToDoList-JS-MiniProject
A clean To-Do List web app built with vanilla HTML, CSS &amp; JS. Supports adding, deleting and marking tasks as done — with smooth animations and a colorful UI. No frameworks, just DOM manipulation.
